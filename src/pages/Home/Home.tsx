import React from "react";
import { Typography } from "@mui/material";

const Home = () => {
  return (
    <Typography
      sx={{
        textAlign: "center",
      }}
      variant="h2"
    >
      FULL-CYCLE EVENT AGENCY{" "}
    </Typography>
  );
};

export default Home;
